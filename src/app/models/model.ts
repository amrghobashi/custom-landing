export interface Description{
    desc?: string;
}

export interface Speaker {
    name?: string;
    title?: string;
    company?: string;
    topic?: string;
    imageUrl?: string;
}

export interface Sponsor{
    image?: string;
}